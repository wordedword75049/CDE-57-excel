language: python

python:
  - 3.6
 
before_install:
  - sudo apt-get install python-tk
      
install:
  - pip install xlsxwriter
  - pip install csv

script:
  - cd GenerateApp
  - python generator.py
